<!--Form for new post-->
<%= form_for(@post) do |f| %>
  
	<!--Return errors here-->
	<% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  	<% end %>

	
    <%= f.label :name, :class => "label" %>
    <%= f.select :name, options_for_select(["Little Buddha", "Grant", "Gus", "Kat", "Ian"])  %>

    <%= f.label :title, :class => "label" %><br />
    <%= f.text_field :title, :class => "input" %>

    <%= f.label :content, :class => "label" %><br />
    <%= f.text_area :content, :class => "new_post_textarea" %>


    <%= f.submit "Submit", :class => "submit_new_post" %>

<% end %>
